---
- name: restart vector
  systemd:
    name: "{{ vector_service_name }}"
    state: restarted
    daemon_reload: yes
  become: yes

- name: reload vector
  systemd:
    name: "{{ vector_service_name }}"
    state: reloaded
    daemon_reload: yes
  become: yes
